import { makeStyles } from '@material-ui/styles'

export default makeStyles({
  drawer: {
    width: 240,
    flexShrink: 0,
  },
  drawerPaper: {
    width: 240,
  },
  component: {
    textAlign: 'center',
    height: '60px',
  },
})
